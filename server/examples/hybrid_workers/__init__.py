"""Hybrid architecture example: a conversational concierge that dispatches
headless work to independent workers via Pipecat's Workers + Bus + Jobs.

See README.md for the diagram, the job contract, and how to graft the
dispatch onto the real voice/FreeSWITCH bot.
"""
