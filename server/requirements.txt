# Pipecat 1.5.0 bundles Flows (pipecat.flows) — the standalone pipecat-ai-flows
# package is no longer used. The `daily` extra pulls daily-python, which has no
# Windows wheels; install/run this (Phase 1, Daily transport) on Linux/WSL.
# fastapi/uvicorn/aiohttp/loguru are transitive deps of pipecat-ai and are left
# unpinned here so pip resolves versions compatible with pipecat 1.5.0.
pipecat-ai[daily,google,deepgram,cartesia,elevenlabs,rime,silero,openai]==1.5.0

# Used directly by the server code.
python-dotenv
pytz
fastapi
uvicorn
loguru
aiohttp
